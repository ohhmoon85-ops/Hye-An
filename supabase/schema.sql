-- =====================================================================
-- 혜안 (HYEAN) — 스키마 진입점
--
-- 스키마의 실체는 migrations/ 안에 있다. 이 파일은 마이그레이션을 순서대로
-- 불러오기만 한다. 정의를 두 곳에 두면 반드시 어긋나므로, 스키마를 바꿀
-- 때는 migrations/ 에 새 파일을 추가하고 아래 목록에 한 줄을 더한다.
-- 대시보드에서 직접 고치지 않는다.
--
--   적용: supabase db push
--   또는: psql "$DATABASE_URL" -f supabase/schema.sql
-- =====================================================================

\ir migrations/0001_init.sql
